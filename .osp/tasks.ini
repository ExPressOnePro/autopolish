[autopolish.local]

Launch Node app = * * * * * 1 0 cmd.exe /c "osp project {host} start"
Backup = */5 * * * * * 0 cmd.exe /c "tar -czf {base_dir}\.osp\backup\{host_decoded}.tar.gz -C {web_root} *"

[forum.autopolish.local]

Backup = */5 * * * * * 0 cmd.exe /c "tar -czf {base_dir}\.osp\backup\{host_decoded}.tar.gz -C {web_root} *"
